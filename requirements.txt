# requirements.txt
# llama-cpp-python removed — not needed for HF Space (--serve --safe mode).
# Inference is handled by an external Ollama endpoint or cloud API via LLM_BASE_URL.
psutil
rich
numpy
PyYAML
aiohttp
colorama
# no extra deps required for StrEnum fallback (handled by sitecustomize.py)
