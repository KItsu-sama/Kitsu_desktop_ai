# DELETED: docs/DEVELOPMENT.md

This placeholder documents that `docs/DEVELOPMENT.md` was removed because it contradicts the current repository architecture/content.

## Deletion reason
- References paths and module layout that do not exist in the repo (e.g. `src/kitsu/...`, `input_mux.py`, etc.), while the actual codebase uses `application/` and `domain/` folders.

## Note
- If you want, you can restore the file from git history.

